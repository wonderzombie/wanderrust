<!--
Template for a PATTERN NOTE. These are different animals from design docs:
no lifecycle, no status, never "done." They're reference. Keep them short.

Copy into docs/patterns/<name>.md. If it grows an Objective and an
Alternatives section, it wasn't a pattern note — it was a design doc.
-->

# PATTERN NAME

**When you want:** the situation, in one sentence. Write this from the
position of someone who has the problem but doesn't know the answer —
that's how they'll find it.

## SOLUTION

The pattern, briefly. Why it works.

```rust
// The smallest code that demonstrates it.
```

## NOTES

Optional. Caveats, costs, when *not* to use it.

<!--
If there's more than one way to skin it, use a second `## SOLUTION`
heading rather than inventing a taxonomy. `notes_on_some_patterns.md`
already does this and it reads well.
-->
